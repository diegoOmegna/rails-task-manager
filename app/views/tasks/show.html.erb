<h1><%= @task.title %></h1>
  <ul>
      <li>
      <%= @task.details %>
      </li>
  </ul>
 <p> <%= link_to "Edit task", edit_task_path %></p>
 <p> <%= link_to "go back", tasks_path(@task) %></p>
    <%= link_to "Delete", task_path(@task),
       data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>
